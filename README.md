# Spotify Wrap Clone (Python)

This workspace contains a starter Python project for building a Spotify Wrap-style application.

## Structure

- `src/spotify_wrap_clone/` – application package
- `tests/` – unit tests

## Run

```bash
python -m src.spotify_wrap_clone.app
```
